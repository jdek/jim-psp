This sample uses the sceUtilitySavedata*() functions.  It only works
from game mode, so it will not work under the TIFF exploit on 2.0.
It also only handles old format saves (0x5c8 byte SaveParam structure).
